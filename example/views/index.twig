{% extends "layout.twig" %}

{% set title = "Tala Payments Example" %}
{% block content %}
    <p>The following gateways are available:</p>

    <ul>
        {% for gateway in gateways %}
            <li><a href="/gateways/{{ gateway }}">{{ gateway }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
